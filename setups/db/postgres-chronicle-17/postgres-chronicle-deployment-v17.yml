apiVersion: apps/v1
kind: Deployment
metadata:
  name: chronicle-db-v17
  namespace: dsm
  labels:
    app: chronicle-db-v17
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chronicle-db-v17
  template:
    metadata:
      labels:
        app: chronicle-db-v17
    spec:
      containers:
        - name: postgres
          image: postgres:17
          envFrom:
            - configMapRef:
                name: postgres-chronicle-config
            - secretRef:
                name: postgres-chronicle-secret
          ports:
            - containerPort: 5432
          resources:
            requests:
              memory: "500Mi"
              cpu: "300m"
            limits:
              memory: "1Gi"
              cpu: "600m"
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-chronicle-pvc-v17
