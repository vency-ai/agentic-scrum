apiVersion: v1
kind: Service
metadata:
  name: project-db
  namespace: dsm
spec:
  selector:
    app: project-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
