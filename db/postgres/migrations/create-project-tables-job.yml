apiVersion: batch/v1
kind: Job
metadata:
  name: create-project-tables-job
  namespace: dsm
spec:
  template:
    spec:
      containers:
      - name: create-tables
        image: postgres:13
        command: ["/bin/bash", "-c"]
        args: 
          - "psql -h project-db -U ${PGUSER} -d ${PGDATABASE} -f /etc/sql/create_project_tables.sql"
        env:
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: project-db-secret
              key: POSTGRES_PASSWORD
        - name: PGUSER
          valueFrom:
            configMapKeyRef:
              name: project-db-config
              key: POSTGRES_USER
        - name: PGDATABASE
          valueFrom:
            configMapKeyRef:
              name: project-db-config
              key: POSTGRES_DB
        volumeMounts:
        - name: sql-script
          mountPath: /etc/sql
      volumes:
      - name: sql-script
        configMap:
          name: project-tables-sql-configmap
      restartPolicy: OnFailure
  backoffLimit: 4
