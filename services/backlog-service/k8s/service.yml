apiVersion: v1
kind: Service
metadata:
  name: backlog-service
  namespace: dsm
spec:
  selector:
    app: backlog-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
